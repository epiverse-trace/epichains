# Validate an `<epichains>` object

Validate an `<epichains>` object

## Usage

``` r
.validate_epichains(x)
```

## Arguments

- x:

  An `<epichains>` object.

## Value

Invisibly returns the object if it is valid.

## Author

James M. Azam
