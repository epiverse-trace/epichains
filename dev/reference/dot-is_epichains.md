# Test if x is an `epichains` object

Test if x is an `epichains` object

## Usage

``` r
.is_epichains(x)
```

## Arguments

- x:

  An R object.

## Value

Logical; `TRUE` if the object is an `<epichains>` and `FALSE` otherwise.

## Author

James M. Azam
