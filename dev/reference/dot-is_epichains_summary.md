# Test if x is an `epichains_summary` object

Test if x is an `epichains_summary` object

## Usage

``` r
.is_epichains_summary(x)
```

## Arguments

- x:

  An R object.

## Value

Logical; `TRUE` if the object is an `<epichains_summary>` and `FALSE`
otherwise.

## Author

James M. Azam
